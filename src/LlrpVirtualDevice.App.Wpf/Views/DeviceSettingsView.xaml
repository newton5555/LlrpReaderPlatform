<UserControl x:Class="LlrpVirtualDevice.App.Wpf.Views.DeviceSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel MaxWidth="800" HorizontalAlignment="Left">
            <!-- 运行中锁定提示 Banner -->
            <Border Background="#FEF3C7" BorderBrush="#F59E0B" BorderThickness="1" CornerRadius="4" Padding="14,10" Margin="0,0,0,16">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <fa:IconBlock Icon="TriangleExclamation" FontSize="16" Foreground="#D97706" Margin="0,0,10,0" VerticalAlignment="Center" />
                    <TextBlock Text="读写器服务正在运行中，基础网络与硬件参数已锁定。如需修改配置，请先点击顶部 “停止” 按钮。" FontSize="12" FontWeight="SemiBold" Foreground="#92400E" VerticalAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- 配置表单卡片 (StandardCardStyle) -->
            <Border Style="{StaticResource StandardCardStyle}">
                <StackPanel>
                    <TextBlock Text="设备基础与网络配置 (Device Configuration)" FontSize="14" FontWeight="Bold" Margin="0,0,0,16" Foreground="{DynamicResource MahApps.Brushes.Text}" />

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="340" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- 实例名称 -->
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="读写器名称:" VerticalAlignment="Center" Margin="0,8" FontWeight="SemiBold" />
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding CanEditConfig}" Margin="0,4" Padding="8,5" />
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="自定义该虚拟读写器的显示名称" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{DynamicResource MahApps.Brushes.Gray2}" FontSize="11" />

                        <!-- 监听地址 -->
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="监听 IP 地址:" VerticalAlignment="Center" Margin="0,8" FontWeight="SemiBold" />
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding EditListenAddress, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding CanEditConfig}" Margin="0,4" Padding="8,5" />
                        <TextBlock Grid.Row="1" Grid.Column="2" Text="支持 127.0.0.1 (本机) 或 0.0.0.0 (局域网)" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{DynamicResource MahApps.Brushes.Gray2}" FontSize="11" />

                        <!-- 监听端口 -->
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="TCP 监听端口:" VerticalAlignment="Center" Margin="0,8" FontWeight="SemiBold" />
                        <mah:NumericUpDown Grid.Row="2" Grid.Column="1" Value="{Binding EditPort}" IsEnabled="{Binding CanEditConfig}" Minimum="1024" Maximum="65535" Margin="0,4" Height="30" />
                        <TextBlock Grid.Row="2" Grid.Column="2" Text="标准 LLRP 端口通常为 5084 / 5085" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{DynamicResource MahApps.Brushes.Gray2}" FontSize="11" />

                        <!-- 模拟型号 -->
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="模拟设备型号:" VerticalAlignment="Center" Margin="0,8" FontWeight="SemiBold" />
                        <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding AvailableProfiles}" SelectedItem="{Binding EditDeviceProfile}" IsEnabled="{Binding CanEditConfig}" Margin="0,4" Height="30" />
                        <TextBlock Grid.Row="3" Grid.Column="2" Text="控制响应的 Manufacturer ID 与型号" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{DynamicResource MahApps.Brushes.Gray2}" FontSize="11" />

                        <!-- 协议版本 -->
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="LLRP 协议版本:" VerticalAlignment="Center" Margin="0,8" FontWeight="SemiBold" />
                        <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding AvailableProtocolVersions}" SelectedItem="{Binding EditProtocolVersion}" IsEnabled="{Binding CanEditConfig}" Margin="0,4" Height="30" />
                        <TextBlock Grid.Row="4" Grid.Column="2" Text="支持 LLRP 1.0.1、1.1 与 2.0" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{DynamicResource MahApps.Brushes.Gray2}" FontSize="11" />

                        <!-- 天线能力由 SDK 能力档案决定 -->
                        <TextBlock Grid.Row="5" Grid.Column="0" Text="天线能力:" VerticalAlignment="Center" Margin="0,8" FontWeight="SemiBold" />
                        <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Config.MaxAntennas, StringFormat='{}能力档案固定为 {0} 个通道'}" VerticalAlignment="Center" Margin="0,8" />
                        <TextBlock Grid.Row="5" Grid.Column="2" Text="由 SDK capability profile 决定，不能单独修改" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{DynamicResource MahApps.Brushes.Gray2}" FontSize="11" />

                        <!-- ROSpec 状态策略 -->
                        <TextBlock Grid.Row="6" Grid.Column="0" Text="ROSpec 状态策略:" VerticalAlignment="Center" Margin="0,8" FontWeight="SemiBold" />
                        <CheckBox Grid.Row="6" Grid.Column="1" IsChecked="{Binding EditRelaxedRoSpecStateChecks}" IsEnabled="{Binding CanEditConfig}" Content="启用宽松状态校验，允许常见生命周期命令幂等执行" Margin="0,6" VerticalAlignment="Center" />
                        <TextBlock Grid.Row="6" Grid.Column="2" Text="重复 Enable、Start、Stop、Disable 可成功；Active 时 Disable 会先隐式 Stop" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{DynamicResource MahApps.Brushes.Gray2}" FontSize="11" TextWrapping="Wrap" />
                    </Grid>

                    <Separator Margin="0,12,0,16" Background="{DynamicResource MahApps.Brushes.Gray8}" />

                    <!-- 保存操作 -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Command="{Binding SaveConfigCommand}" IsEnabled="{Binding CanEditConfig}" Style="{StaticResource PrimaryActionButtonStyle}" Width="120" Height="34">
                            <StackPanel Orientation="Horizontal">
                                <fa:IconBlock Icon="FloppyDisk" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center" />
                                <TextBlock Text="保存并应用" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
